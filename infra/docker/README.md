# Refer to root docker-compose.yml for full orchestration
# This directory is maintained for infrastructure documentation and deployment manifests.
# Use: docker compose -f ../../docker-compose.yml up -d
